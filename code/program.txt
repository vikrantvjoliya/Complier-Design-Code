#include <bits/stdc++.h>
using namespace std;
int main() {
        long n, k;
        cin >> n >> k;
        n = n + k ;

        if ( n > 0)
                break;
        else
                return;
        }
return 0;
}